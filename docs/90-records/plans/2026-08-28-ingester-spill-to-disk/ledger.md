# SDD ledger — plan: docs/90-records/plans/2026-08-28-ingester-spill-to-disk/plan.md

*(Ledger nằm trong thư mục plan theo CLAUDE.md §4.1 — artifact tạm của subagent ở scratchpad ngoài repo, không tạo `.superpowers/`.)*

## Quét tiền-thực-thi (2026-08-27 tối)

Cặp task chung file/interface:

| Cặp | Produce ↔ Consume | Kết quả |
|---|---|---|
| T1 ↔ T5 (`chwriter.py`) | T1 thêm timing/gauge trong `flush_once`; T5 viết lại thành `manage_once`/`write_once` nhưng Interfaces T5 tuyên giữ `insert_percentiles`, và test T5 assert gauge cập nhật trong `manage_once` | Khớp — gauge dời vào vòng quản, `flush_once` compat = manage+write nên hành vi T1 giữ |
| T4 ↔ T6/T7/T8 (`SpillStore`) | `write(table, block, kind)` / `next_batch(max_rows)` / `SpillItem` / `delete` / `empty` / `counters` | Chữ ký thống nhất ở cả 4 task |
| T4 ↔ T8 (`config.spill_dir`) | T4 thêm trường; T8 `_run_run` dùng `cfg.spill_dir` | Khớp |
| T5 ↔ T6 (`_Pending`, hằng số) | T5 produce `_Pending(table, block, first_try)`; T6 test import `_Pending`, `N_CAP_ROWS` | Khớp (T6 mới thêm N/K/SPILL hằng — T5 chưa cần) |
| T5 ↔ T8 (`main.py`) | T5 đổi loop + drain dùng `clean()`; T8 đổi budget mặc định + replay nợ | Sửa tuần tự cùng file, không đè nhau |
| T9 ↔ T1 (`make_on_packet`) | T9 rút `process_record` nhưng giữ frames-counter + `not_leader_dropped` của T1 | Test i12 là lưới hồi quy |
| T2 ↔ T3 (probe → hằng số) | Gate T3 (bản điều chỉnh tối 27/08) cần p95 insert từ probe | **Ruling PF-1** dưới |

Tự-nhất-quán từng task: T5 test dùng `WRITE_CALL_BUDGET_S`, `_Clock` — định nghĩa trong cùng file test ✓ · T6 `_writer_with_spill` cần `import time` — file test đã import ✓ · T8 `drain_writer` import từ `ingester.main` ✓ · T10 khung chaos có hai chỗ chủ đích giao executor (client factory sau docker start) — có nêu ranh giới assert ✓.

**Ruling PF-1:** mục "thêm phép đo p95 insert vào probe" nằm ở gate Task 3 nhưng là việc code — gộp vào dispatch Task 2 (probe một file, một lần chạy). Chi phí nếu sai: probe dài hơn vài phút, không ảnh hưởng gì khác.

**Ruling PF-2:** plan Task 5 bước 4 đòi sửa test cũ hai lần (T5 giữ drop, T6 đổi thành spill). Chấp nhận chi phí sửa-hai-lần để mỗi task tự xanh độc lập — đúng nguyên tắc task-tự-kiểm của plan. Chi phí nếu sai: vài phút công subagent.

## Tiến trình

- Task 4: implementer DONE (672265f, 8 test mới, suite 113/113; sửa kèm 2 call site `Config` trong test_i10 — hệ quả tất yếu của field bắt buộc mới, hợp lệ). Review: Approved + 1 Important (plan-authored: `.corrupt` không trừ `bytes_used` → trần đĩa từ chối oan sau sự cố hỏng file, fail-safe nhưng thật) + 1 Minor (tmp không dọn ở nhánh OSError). **Ruling T4-1:** cả hai là lỗi của code trong plan — sửa, không park.
- Task 4: fix round 1/5 (2 addressed, 0 open — bytes_used trừ đúng sau corrupt-rename có kiểm chống double-decrement; tmp dọn ở nhánh OSError; commits 672265f..7fe9a17, re-review pass, 115/115)
- Task 4: minor (deferred): TOCTOU hẹp ở `stat()` trong nhánh corrupt (re-reviewer ghi nhận, không thành finding dưới mô hình single-owner-lock)
- Task 4: complete (commits 7502fbc..7fe9a17, review clean sau 1 fix round)
- *(Chỉ đạo chủ dự án giữa phiên: task khó thì escalate Opus — chốt: T6/T8 dispatch thẳng Opus; reviewer các task đĩa/hồi phục cũng Opus.)*
- Task 5: implementer DONE (0c460f6, 119 pass; Sonnet tự quyết 3 điểm: `_write_lock` non-blocking riêng cho write_once, `_last_exc` chuyển exception ra ngoài để log, adapt thêm test_i10 + 3 test transient ngoài danh sách brief — đều disclose). Review Opus: Needs fixes — 2 Important THẬT: (1) `queue_rows -= 1` ngoài `_lock` ở nhánh poison (race lost-update; T6 dùng counter này làm đầu vào cửa chế độ đĩa); (2) `drain_writer` retry 10 Hz không backoff (~600 lần đập server đang ngộp trong 75 s). 7 trục concurrency còn lại đều sạch (đã trace tay). Fix round 1/5 (2 addressed, 0 open — queue_rows: cả 5 điểm mutation audit dưới _lock; drain: sleep 1s không bypass, fast-path clean giữ nguyên; test drain đỏ thật 752→≤77, test queue_rows là invariant-assertion theo fallback đã duyệt; commits 0c460f6..353c57b; 121 pass). 4 cleanup optional đã áp, re-review xác nhận không vỡ mới.
- Task 5: complete (commits 7fe9a17..353c57b, review clean sau 1 fix round)
- Task 6: implementer (Opus) DONE_WITH_CONCERNS (824eaed, 126 pass; tự bắt + sửa kèm test đỏ một race thật do code mới: `_enter_disk` rebind `self.queue` khi write thread đang giữ block peek — sửa bằng remove-theo-identity). **Ruling T6-1 (C-1):** cửa 2 chỉ bắn khi có spill sở hữu được; cấu hình suy giảm (spill=None/không own) giữ drop-theo-hạn-chót cho block già nhất dưới counter MỚI `no_spill_dropped.<table>` + ledger log cấu trúc — vì gate thuần (giữ nguyên T5) là tái mở đường phình RAM vô trần, còn contract nguyên bản (vào disk mode không đĩa) là vứt sạch mọi dòng suốt sự cố ở tải nhẹ trong khi RAM còn dư. Cửa 1 giữ vô điều kiện. Chi phí nếu sai: một đường drop có sổ tồn tại thêm ở cấu hình không-spill — spec §2.3.2 nói "xoá dropped_block" nhưng §6 đã định nghĩa last-resort có đếm; đây là diễn giải theo tinh thần §6. Fix round đang chạy. Deviation chấp nhận: `_isolate_poison` chung, poison phía head, `replay_blocks` đếm theo file; `spill_bytes` gauge hoãn sang Task 8 (PHẢI nhớ đưa vào dispatch T8).
- Task 5: minor (deferred): `self.sleep` là thuộc tính chết (ctor đóng băng theo Interfaces) — để whole-branch review quyết. *(4 minor khác của review Opus đã sửa trong fix round 1.)*
- Task 6 review (Opus): Needs fixes — **1 CRITICAL: guard `_enter_disk` ngoài lock + set flag sau khi nhả lock → hai cửa trên hai thread cùng qua, head đông cứng bị đè lặng lẽ (mất tới N_CAP_ROWS dòng không đếm)**; 3 Important: (I1) `_drain_disk_step` đọc/XOÁ file spill không kiểm `.owned` — tiến trình thua lock có thể rút ruột thư mục của chủ đang sống; (I2) `_drain_ram` không dừng khi mode lật giữa call 5s → đua với `_spill_tail`, trùng tránh được; (I3) split fail không `return` → parent bị re-insert/re-split mỗi vòng, đẻ orphan child mỗi lượt. Phép đi bộ bảo toàn dòng đơn-thread SẠCH cả 6 đích; test adapt giữ nguyên bài học cũ. **Ruling T6-2:** M3 (mirror `replay_corrupt` chỉ một lần lúc adopt — sự kiện MẤT dòng không vào metrics) nâng thành bắt buộc theo tinh thần §6; M4 (owned-trước-scan trong spill.py) chuyển Task 8; M1 giữ deferred SAU khi I3 sửa; M2: đính chính C-4 — `empty()` không chạy mỗi giây ở RAM mode (chỉ khi chưa own hoặc disk-mode-sắp-thoát), chi phí thật là `_files()` trong `next_batch`, chấp nhận được ở ~9k file. Fix round 1/5 (5/5 addressed — C1 check-and-set atomic trong _lock, thứ tự lock `_write_lock`→`_lock` một chiều không deadlock; I1 gate owned cho next_batch/delete, intruder vẫn thấy file chủ nên không thoát mode sớm; I2 kiểm disk_mode mỗi vòng, test đã đổi sang trade block sau khi tự bắt xanh-giả; I3 `_split_disk_item` trả bool + return; M3 mirror mỗi nhịp; commits 8b92c9b..04981e4; 132 pass; C1 test race đo thật 8/8 đỏ trên bản lỗi).
- Task 6: complete (commits 353c57b..04981e4, review clean sau 1 fix round — Critical mất-head-im-lặng đã diệt)
- Task 7: implementer DONE (0d8167f, 134/134) — chỉ test: cơ chế K + FIFO đã đúng sẵn từ T6, hai test chứng minh biết-đỏ bằng đột biến tạm ([3,8]≠[3,2] và [1,3,2]≠[1,2,3]) rồi revert.
- Task 7: complete (commits 04981e4..0d8167f, review Approved — 0 finding, số K trace tay độc lập khớp)
- Task 8: implementer (Opus) DONE (b3828b4, 149 pass; +15 test). **Ruling T8-1:** deviation client-reconcile-riêng CHẤP NHẬN — writer client mang `send_receive_timeout=20s` mà reconcile là FULL OUTER JOIN cả ngày, đúng bẫy đã ghi trong main.py; kèm wrap ClickHouseError để verdict hôm qua hỏng không chặn phiên hôm nay. M4 sửa tận gốc (owned đặt SAU scan; phát hiện thêm: anti-clobber so tên gồm kind nên bản lỗi mint hai file cùng seq mà `seq_collision`=0). Hai gap tự tìm + sửa đỏ-trước: replay_debt có thể return còn file mà không vào disk mode (đường poison/IO) → vỡ FIFO + verdict không tin vĩnh viễn; OSError đĩa lúc boot thoát traceback trần vượt hợp đồng exit-3. Flag ngoài phạm vi: drain chỉ chạy khi is_leader → mất leadership giữa phiên thoát với nợ đĩa mà drained=True (chuyển whole-branch review + cân nhắc T11 ghi chú tài liệu). Smoke thật `--minutes 1` exit 0 (§3.5).
- Task 8 review (Opus): Needs fixes — 2 Important MỚI trên seam boot: (I1) exit-3 lách được qua mkdir/open/unlink/stat của đường spill-boot + `finally` của replay_debt chạy SAU except nên OSError từ empty() thoát; trigger Windows thật: AV giữ handle .tmp → PermissionError; (I2) M4-reorder tạo zombie lock — `_scan` ném giữa try_acquire thì tiến trình GIỮ lock OS với owned=False vĩnh viễn, không ai adopt được. Bảo toàn dòng qua replay_debt SẠCH mọi đường; M4 root-fix + bỏ scan lặp trong adopt là fix đúng. **Ruling T8-2:** `_maybe_exit_disk` thiếu `.owned` (pre-existing T6, intruder kẹt disk-mode vĩnh viễn + verdict sai trong khi clean()=True) — một điều kiện, sửa ngay round này thay vì đẩy branch-review. **Ruling T8-3:** nhánh non-leader cuối phiên phải `drained = writer.clean()` (mất leadership giữa phiên hiện thoát với nợ đĩa mà verdict in như đáng tin — lỗi báo cáo, không mất dòng vì replay_debt sáng sau vớt lại). Minor deferred: timeout reconcile nợ vô hạn theo driver 300s/ngày (giao optional), test today không freeze (optional), test log drain chỉ pin "còn" (optional), red wave-1 kiểu ImportError ngang (ghi nhận quy trình). Fix round 1/5 (4/4 addressed — I1 boot block exit-3 + finally OSError-safe mặc-định-còn-nợ; I2 `_release()` đóng handle cả hai nhánh + re-raise (chọn re-raise đúng: return False sẽ báo nhầm đĩa hỏng thành "người khác giữ lock"); I3 gate `.owned`, owner thật không đổi hành vi; I4 `_drain_for_verdict` non-leader trả clean() không insert; 3 optional đều áp; commits b3828b4..daf5014; 154 pass; smoke thật lại exit 0).
- Task 8: complete (commits 0d8167f..daf5014, review clean sau 1 fix round)
- Task 8: minor (deferred): `drain_writer` gọi `manage_once` không guard OSError — leader chưa-từng-own có thể propagate qua đuôi `_run_run` bỏ qua reconcile (pre-existing, 3 điều kiện trùng mới kích; fix đúng chỗ là guard đuôi `_run_run` — whole-branch review quyết).
- Task 9: implementer DONE (457ce25, 156/156; 2 golden đỏ→xanh). Self-flag: `_default_day_window` + SQL `--db` (fromUnixTimestamp64Milli) suy từ DDL, CHƯA chạy trên CH thật — sẽ được nghiệm thật ở gate AC3 phiên 28/08.
- Task 9: complete (commits daf5014..457ce25, review Approved — refactor xác nhận behavior-identical bằng trace + chạy lại 156/156 độc lập; golden dup chứng minh phụ thuộc đồng hồ `r` thật; SQL --db phân tích tĩnh sạch TZ). Minor deferred: `_iso_to_ms` nuốt offset tường minh nếu ai truyền +00:00 (brief chỉ định naive-local); --db cần một lần chạy thật trước AC3 (controller sẽ diễn tập tối nay).
- **Diễn tập AC3 (controller, 2026-08-27 20:32, cửa sổ 10:00–10:05 phiên 27/08 thật):** trade 3570=3570 · quote 53591=53591 · index_delta 810=810 · pt_match 6=6 (diff 0 tuyệt đối); snapshot_delta 14698 vs 14697 (−1 — đúng dạng chênh-hai-socket mà AC3 sẽ hạch toán); dup_dropped=14 tái hiện đúng bằng đồng hồ `r`. SQL `--db` chạy đúng trên CH thật kể cả TZ — minor "chưa chạy thật" của Task 9 ĐÓNG.
- Task 10: implementer DONE sau 2 run + 3 lần treo-chờ-nền (giống ca Task 2 — agent Sonnet có xu hướng background dài rồi ngồi đợi; run cuối tự hoàn tất qua poller riêng của nó). **AC2 PASS THẬT: fed 200.000 / stored 200.000 / dup 0 / RSS đỉnh 78,7 MB (trần 200) / 253 block spill+replay qua docker stop thật.** Run 1 fail vì plumbing test (stub `_trade_normalized` của brief để ts=None — chết trên CH thật; thay bằng `_valid_trade`). Ngoài phạm vi được flag thành background chip: lỗi serialize phía client không mang mã CH có thể bị phân loại transient retry mãi. Commit 232b666 (kèm psutil dev-dep). Counter run PASS: `spill_blocks=253 · spill_rows=126.499 · replay_blocks=253` — dup=0 không phải may mắn: `-r` phát lại nguyên hash + cửa sổ dedup 100 block của `rt.trade` (MergeTree thuần nên count() phản ánh đúng kho). Suite 156 passed 1 skipped (file chaos tự skip khi thiếu RUN_CHAOS).
- Task 10: complete (commits 457ce25..232b666, review Approved — 0 Critical/Important; minor "paste ledger" do controller nhận, đã làm ở dòng này)
- Task 6: minor (deferred): `_maybe_exit_disk` set `disk_mode=False` ngoài _lock (re-reviewer ghi nhận ngoài phạm vi — chiều thoát lành tính vì chỉ manage thread set và enter đã atomic; whole-branch review soi lại) · M1/C-3 (orphan child khi split fail transient — một nửa-block trùng mỗi tick, có `replay_blocks` làm dấu, cần mở rộng API SpillStore mới sửa được) · M4 → dispatch T8 · M5 `_disk_blocks` ngoài lock (cosmetic, chủ đích).

*(cập nhật sau mỗi task)*

- Task 11: complete (commits 232b666..fa771dc, review Approved — mọi số trace được về code/spec, grep §1.7 sạch, vùng lịch sử nguyên vẹn)
- **Whole-branch review (Opus): MERGE-READY WITH MUST-FIX.** 3 trace vòng đời đều HOLD; không ruling nào unsound. Must-fix (1 fix wave): (1) `_drain_for_verdict` không guard → OSError cuối phiên giết luôn reconcile; (2) reconcile ngày nợ chạy cả khi nợ CHƯA xả sạch → verdict tự tin sai đè lên "KHÔNG ĐÁNG TIN" trung thực (vi phạm §5.3); (3) `_load` trộn OSError với corruption → block lành bị `.corrupt` mất dòng hoặc kẹt vòng; (4) `SpillStore.write` gọi từ 2 thread không lock — race seq làm loser xoá tmp của winner. **Phát hiện lớn: đòn bẩy gộp KHÔNG ăn trong interleave 5 bảng thật (run cùng-bảng dài 1) → biên khả thi K ≈2,8× chứ không phải 8,7× — vẫn đạt, nhưng số 8,7× là số giả định phải đính chính (§1.2); AC2 không thấy vì chaos chỉ feed trade.** AC3-prep: `not_leader_dropped` per-table + counter `replay_rows` mới. Triage minor hoãn: T4 fold vào fix 3; T8-drain widen vào fix 1; T9 `_iso_to_ms` DEFER nhưng NHỚ khi chạy AC3 (đừng gõ offset); còn lại DEFER có lý do — bảng đầy đủ trong report reviewer. Docs: docstring test_i08 còn tả hợp đồng cũ (§1.7); §3.7 cần 3 dòng định giá trùng (bar_1m sumState phồng vĩnh viễn → P1 đầu sau replay đọc là "trùng dự kiến"). **Khuyến nghị hàng đầu: smoke §3.5 lại trên HEAD cuối vì 457ce25 đã refactor đường run sau smoke gần nhất.** Fix wave (Opus) đang chạy.
- Fix wave cuối: DONE (5eda0d9, 164 pass 1 skip; 10 test đỏ-trước, race write-lock tái hiện đúng WinError 32 dự đoán rồi mới sửa). Hai đổi ngữ nghĩa flag cho re-review: mirror counter set→inc-by-delta (spill_io_error có 2 nguồn), replay_corrupt chỉ đếm quarantine thành công. Biên 2,8× ghi là SỐ SUY RA trong spec §2.5 + comment K.
- **Smoke §3.5 trên HEAD cuối (controller, 21:53): EXIT=0** — boot spill mới + subscribe 6.081 topic + giành leader + reconcile p1=0 p2=0 ok=868; counter mới (orphan_tmp/replay_corrupt/seq_collision/spill_io_error/spill_bytes) đều hiện, đều 0. Khuyến nghị hàng đầu của whole-branch review ĐÃ THỰC HIỆN.
- Re-review fix wave: **15/15 ADDRESSED, 2 đổi ngữ nghĩa đều sound, không vỡ mới** — verdict merge. Controller sửa nốt 2 dòng sót quét trong spec (§1 phạm vi + công thức AC3: `not_leader_dropped.<table>` + ghi `replay_rows` vào vế trùng).
- **Parked (ruling cuối, theo triage của hai reviewer):** (P1) file hỏng-vĩnh-viễn ở đầu hàng đợi đĩa chặn drain không giới hạn retry — HỮU HÌNH (spill_io_error + log mỗi giây + verdict untrusted + sáng sau từ chối reconcile) và không mất dòng; follow-up tự nhiên là `.stuck` quarantine sau N tick + rate-limit log; (P2) `counters[...] += 1` trong `_load` ngoài `_mutex` — chỉ lệch số đếm, không đụng đường dữ liệu; (P3) log "đã phát lại xong nợ đĩa" gây hiểu nhầm khi dừng vì `_IO_ERROR` (gate §5.3 làm hậu quả vô hại); (P4) `seq_collision` không advance seq tới lần `_scan` kế. Cả 4 để lát sau / lượt sửa kế tiếp đụng file đó.
- **HOÀN TẤT GUỒNG 2026-08-27 đêm:** 11 task + 1 fix wave cuối, 6 fix round tổng, 164 test (156→164) + 1 chaos gated; AC1 ✅ (suite) · AC2 ✅ (200k/200k, dup 0, RSS 78,7 MB, 253 block qua đĩa) · AC4 ✅ (hằng số + căn cứ trong code; biên 2,8× ghi là số suy ra) · **AC3 chờ phiên 28/08** (lệnh trong plan Task 12; nhớ: KHÔNG gõ offset TZ vào --from/--to).

- Task 1: implementer DONE_WITH_CONCERNS (commit 3f2cc33, 105 pass/73s). Concern đúng: test gauge trong plan thiếu fake clock → tự đốt 60s thật mỗi lần chạy suite. **Ruling T1-1:** lỗi do plan viết test verbatim thiếu fake clock — sửa trước khi review (resume implementer, thay riêng test đó bằng bản fake clock, assert giữ nguyên). Chi phí nếu sai: không — assert không đổi.
- Task 1: minor (deferred): (a) chưa có test cho nhánh WARN-log BẮN (chỉ test nhánh im — plan-mandated); (b) `insert_percentiles()` nhánh deque rỗng chưa test; (c) ledger.md bị cuốn vào commit 272c70a (vệ sinh commit). Hai ⚠️ reviewer đã được controller xác nhận không phải gap (`_run_measure` không có ChWriter; ledger do controller viết).
- Task 1: complete (commits ceeb5dd..272c70a, review clean — Approved, 0 Critical/Important)
- Task 2: implementer DONE sau 2 lần resume (agent tự treo vì background probe rồi ngồi chờ — lần 3 ép foreground + chẻ `-k` mới xong). Commits 7d65d44 + 5219dcb. Số đo: 1a: count=100 trên block 100 dòng insert 2 lần (dedup GIỮ qua pickle roundtrip) · 1b: count=100 trên block 50 dòng (NHÂN ĐÔI sau 105 block chen — đúng dự đoán spec, cửa sổ đẩy theo block) · 1c: count=10 trên block 10 dòng sau 130 s chờ (cửa sổ KHÔNG co theo giây — thuần block) · pickle 5.000 dòng = 323.657 B (~65 B/dòng trên đĩa) · p95 insert: block 5.000 dòng ~88 ms, block 50 dòng ~72 ms, DEV ≈ VPS. **Phát hiện ngoài dự kiến: `memory-vps.xml` như đã commit làm CH crash-loop lúc boot (BAD_ARGUMENTS — 3 setting merge_tree mặc định > pool size 8); đã sửa (pin = 4) + kiểm bằng boot container thật.** Đang review.
- Task 2 review: Needs fixes với đúng 1 Important — số đo chưa vào spec §9/ledger (Bước 3 brief). **Ruling T2-2:** Bước 3 vốn trùng với việc gate Task 3 giao cho controller ("ghi số vào spec §2.5", "KHÔNG giao subagent") — chuyển Bước 3 về gate, controller ghi ngay trong cùng lượt thay vì mở fix round. Chi phí nếu sai: không — cùng nội dung, người ghi khác.
- Task 2: minor (deferred): (a) `import time` lặp cục bộ trong probe; (b) cleanup timing probe không try/finally; (c) `n_1c` không có assert guard (kế thừa từ brief).
- Task 2: complete (commits 272c70a..5219dcb, review: probe logic sạch toàn bộ, Important duy nhất xử bằng Ruling T2-2)
- **GATE Task 3 — ĐÓNG 2026-08-27 tối:** hằng số tạm ĐỨNG VỮNG trước số đo, không đổi giá trị: `N_CAP_ROWS=100_000` (49,7 MB; ~15 s ATO đỉnh) · `K_REPLAY_ROWS=20_000` (>19.488=đỉnh×3; 4 insert gộp×88 ms≈0,35 s<1 nhịp) · `SPILL_CAP_BYTES=10 GiB` (2 h đỉnh ×3 ≈ 9,1 GB @ 65 B/dòng đo). **Điều kiện khả thi §2.4 ĐẠT dư ~8,7×** (6.496×17,6 µs≈0,114<1). p95 VPS hẹp ≈ dev ⇒ K không cần hiệu chỉnh. Số + nguồn đã ghi spec §2.5 + §9. Mục kiểm chứng sau phiên 28/08 giữ nguyên trong plan Task 3.
- **Ruling T2-1:** brief Task 2 bước 2 viết "bật lại compose với overlay VPS" — nhưng probe chạy trên container test ephemeral, overlay compose dev không chạm tới nó, còn trỏ probe vào CH dev thì ghi rác vào kho thật. Quyết: probe tự dựng container CH thứ hai mount `deploy/infra/clickhouse/memory-vps.xml` + `--memory` theo trần vps để đo hồ sơ hẹp, không đụng compose dev. Chi phí nếu sai: probe đo trên môi trường lệch cấu hình VPS thật một phần (mount config là chính, chấp nhận).
